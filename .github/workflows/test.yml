name: Lint

on:
  workflow_call:
  workflow_dispatch:
  pull_request:
  push:
    branches-ignore:
      - master
      - alpha

jobs:
  lint:
    name: Lint
    uses: tksst/github-actions-workflows/.github/workflows/nodejs-pnpm.yml@60c5a837e7d460d37847306dd77c8df8ab7d06c6
    with:
      build: false
      test: false
      os: '["ubuntu-latest"]'
      node-versions: "[18]"
